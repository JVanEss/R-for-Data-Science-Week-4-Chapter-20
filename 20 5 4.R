#20.5.4 Exercises
#Exercise 1
list(a, b, list(c, d), list(e, f))
list(list(list(list(list(list(a))))))
#Honestly, I am lost on this
#Exercise 2
#a tibble is similar to a list except lists can have different lengths of vectors